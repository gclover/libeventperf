import os
import glob

CLIB_HOME = os.environ['CLIB_HOME']

cpppath = CLIB_HOME + '/include'
libpath = CLIB_HOME + '/lib'

Program('target/perf', glob.glob('src/*.cpp'), CPPPATH = [cpppath], LIBPATH = [libpath], LIBS = ['PocoFoundation', 'event'], CCFLAGS = [])
